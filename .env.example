# OpenAI API Configuration
# Get your API key from https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your-api-key-here

# Set to 'true' to use OpenAI's Whisper API (faster, more accurate but requires API key)
# Set to 'false' to use local Whisper model (slower but works offline)
USE_OPENAI_API=false

# Only used when USE_OPENAI_API is false
# Options: tiny, base, small, medium, large (larger models are more accurate but slower)
WHISPER_MODEL=base

# Server Configuration
FLASK_DEBUG=true
FLASK_ENV=development

# Optional: Set a specific port (default is 5000)
# PORT=5000

# Optional: Set host (default is 0.0.0.0)
# HOST=0.0.0.0
